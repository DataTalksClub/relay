services:
  db:
    image: postgres:17
    environment:
      POSTGRES_DB: relay
      POSTGRES_USER: relay
      POSTGRES_PASSWORD: relay_password
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U relay -d relay"]
      interval: 5s
      timeout: 5s
      retries: 5

  web:
    build: .
    ports:
      - "8001:8000"
    environment:
      DATABASE_URL: postgresql://relay:relay_password@db:5432/relay
      DEBUG: "1"
      SECRET_KEY: dev-secret-key-change-in-production
      ALLOWED_HOSTS: localhost,127.0.0.1,web,relay-web
      CSRF_TRUSTED_ORIGINS: http://localhost:8001,http://127.0.0.1:8001
      PUBLIC_BASE_URL: http://localhost:8001
      RELAY_API_DOCS_BASE_URL: http://localhost:8001
      EMAIL_BACKEND: django.core.mail.backends.console.EmailBackend
    depends_on:
      db:
        condition: service_healthy
    command:
      - sh
      - -c
      - |
        uv run python manage.py migrate
        uv run python manage.py seed_demo_data
        uv run python manage.py runserver 0.0.0.0:8000
    healthcheck:
      test:
        [
          "CMD",
          "uv",
          "run",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health/', timeout=2).read()",
        ]
      interval: 5s
      timeout: 5s
      retries: 20

  worker:
    build: .
    environment:
      DATABASE_URL: postgresql://relay:relay_password@db:5432/relay
      DEBUG: "1"
      SECRET_KEY: dev-secret-key-change-in-production
      EMAIL_BACKEND: django.core.mail.backends.console.EmailBackend
    depends_on:
      db:
        condition: service_healthy
    command: ["python", "manage.py", "db_worker", "--interval", "1"]

  scheduler:
    build: .
    environment:
      DATABASE_URL: postgresql://relay:relay_password@db:5432/relay
      DEBUG: "1"
      SECRET_KEY: dev-secret-key-change-in-production
      EMAIL_BACKEND: django.core.mail.backends.console.EmailBackend
    depends_on:
      db:
        condition: service_healthy
    command: ["python", "manage.py", "run_relay_scheduler", "--interval", "5"]

  localstack:
    image: localstack/localstack:4.4.0
    profiles: ["aws-local"]
    ports:
      - "4566:4566"
    environment:
      SERVICES: sqs
      ACTIVATE_PRO: "0"
      AWS_DEFAULT_REGION: us-east-1
      DEBUG: "0"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:4566/_localstack/health"]
      interval: 5s
      timeout: 3s
      retries: 20

volumes:
  postgres_data:
